spring:
  application:
    name: alert-case-service
  datasource:
    # Changed from postgresql to mysql
    url: jdbc:mysql://localhost:3306/alert_case_db?createDatabaseIfNotExist=true
    username: root          # Put your MySQL username here
    password: Rana@2004  # Put your MySQL password here
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

external:
  reporting-service:
    url: http://localhost:8088/sar/ingest-report

server:
  port: 8085

# swagger-ui custom path
springdoc:
  swagger-ui:
    path: /swagger-ui.html